#!/bin/bash

# startClient.sh takes a host name as its first command-line parameter
# and a port number as its second command-line parameter and attempts
# to connect to the server with the given host name and port number.
python3 client.py $1 $2